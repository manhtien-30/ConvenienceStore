<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_main"
    app:startDestination="@id/attendcheck">
    <fragment
        android:id="@+id/profile"
        android:name="com.example.checkattendance.Fragments.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />
    <fragment
        android:id="@+id/attendcheck"
        android:name="com.example.checkattendance.Fragments.AttendCheckFragment"
        android:label="Attendance Check"
        tools:layout="@layout/fragment_attend_check" />

</navigation>